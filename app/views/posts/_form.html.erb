<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

        <div class="nvisible">
    <%= form.label :avatar%>
    <%= form.text_field :avatar, value: current_author.avatar%>
  </div>

  <div class="actions" style="text-align: center">
    <%= form.submit %>
  </div>
    <div class="invisible">
    <%= form.label :name%>
    <%= form.text_field :name, value: current_author.full_name , readonly: true%>
  </div>


<% end %>
