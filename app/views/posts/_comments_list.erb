<script>
  $(function(){
     $(".edit_comment").click(function(){
      var $parent = $(this).parent('.comment_form_parent');
      var $comment = $parent.find('.update_comment');
      var $button = $(this);
      console.log($parent, $comment, $button)
      $comment.removeClass("invisible")
      $button.addClass('invisible')
    })
  })
</script>


<% @comments.select(&:root?).each do |comment| %>
  <br/>
    <%= render 'posts/comment_one', comment: comment%>
<% end %>